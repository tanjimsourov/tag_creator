CONSTRAINTS (apply to every task):
- NO SQLite or any DB engine. Flat files only. Final enriched output stays CSV.
- Cache/state files must be APPEND-ONLY (no full-file rewrites) to scale to 100k+ files.
- Do NOT touch .env, .gitignore, or secret handling. Leave credentials exactly as-is.
- Only use the free/open providers already wired (iTunes, Deezer, MusicBrainz, Spotify,
  Last.fm, Discogs, Cover Art Archive, AcoustID, Wikidata) + open-source local AI audio models.
- Preserve dry-run-first safety, confidence scoring, and resume behavior.
- Windows + Linux compatible. Keep the clean layered architecture.

COMMAND 04 — Fail-fast config validation (no more opaque tracebacks)

Add a validation layer to tag_creator/config.py.
- Wrap every numeric/bool/list/mapping parse so a bad value raises a clear ConfigError:
  "Invalid value for MIN_WRITE_CONFIDENCE: 'abc' (expected a number 0.0-1.0)".
- Range-check: confidences in [0,1]; threads >= 1; timeouts > 0; weights/rates non-negative.
- Warn (not crash) on missing model paths / input dir when local AI is disabled.
- Add a --check-config flag to scripts/enrich_library.py that prints resolved, validated
  settings + provider readiness and exits, running nothing.
Acceptance: a malformed .env prints ONE actionable line and exits non-zero — never a stack trace.
